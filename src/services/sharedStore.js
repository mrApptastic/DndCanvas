import { ref } from 'vue'

// Shared store for grid settings
export const squareSize = ref(40)
export const zoom = ref(0.4)
export const offsetX = ref(0)
export const offsetY = ref(569)
export const showGrid = ref(false)
export const panMode = ref(false)
